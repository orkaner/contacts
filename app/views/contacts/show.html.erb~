<p id="notice"><%= notice %></p>

<table>
<tr>
<th><%= link_to "First Name",:sort=> @contact.first_name %></th>
<th><%= link_to "Last Name",:sort=> @contact.last_name %></th>
<th><%= link_to "Address",:sort=> @contact.address %></th>
<th><%= link_to "Phone Number",:sort=> @contact.phone_number %></th>
<th><%= link_to "E-Mail",:sort=> @contact.email %></th>
</tr>
<% for contact in @contacts %>
<tr>
<td><%= contact.first_name %></td>
<td><%= contact.last_name %></td>
<td><%= contact.address %></td>
<td><%= contact.phone_number %></td>
<td><%= contact.email %></td>
</tr>
<% end %>
</table>
<p>
  


<%= link_to 'Edit', edit_contact_path(@contact) %> |
<%= link_to 'Back', contacts_path %>
